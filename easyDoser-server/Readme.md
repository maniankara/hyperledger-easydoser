## Instructions to setup server

- Make sure you have Golang installed properly and a fabric network running with a cli container named cli.
- Run `go get -u github.com/gorilla/mux`
- Then `go build`
- Finally `sudo ./easdoser-server`